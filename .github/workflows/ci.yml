name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
      - uses: DeLaGuardo/setup-clojure@13.4
        with:
          cli: latest
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Test CLJC
        run: clojure -X:test
      - name: Lint
        run: clojure -M:lint
      - name: Test npm package script
        run: npm test
      - name: Install npm dependencies (nbb, for the bin wrapper)
        run: npm install
      - name: Smoke npm bin wrapper
        run: npx nbb bin/kotoba-office-style.cljk
